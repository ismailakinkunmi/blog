<h1>list of all users</h1>

<div>
  <% @users.each do |user| %>
  <h4>
   <%= link_to user_path(user.id) do %>
     <%= user.name %>
    <% end %>  
  </h4>
   
    <span>Number of posts: <%= user.post_counter %> </span>
  <% end %>
</div> 
